apiVersion: apps/v1
kind: Deployment
metadata:
  name: cards-auth-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cards-auth-mongo
  template:
    metadata:
      labels:
        app: cards-auth-mongo
    spec:
      containers:
        - name: cards-auth-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: cards-auth-mongo-srv
spec:
  selector:
    app: cards-auth-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
